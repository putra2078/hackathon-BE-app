version: '3.8'

services:
  backend:
    build: .
    container_name: hackathon-backend
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    restart: always
